var arr = [];

arr[0] = "zero";
arr[1] = "one";
arr[2] = "tow";

for (var i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}

var arr = ["zero", "one", "tow"];

for (var i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}

var arr = [, , ,];

for (var i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}

// 배열 생성 (빈 배열)
var arr = new Array();

arr[0] = "zero";
arr[1] = "one";
arr[2] = "tow";

for (var i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}

// 배열 생성 (초기 값 할당)
var arr = new Array("zero", "one", "tow");

for (var i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}
